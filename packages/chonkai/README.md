# chonkai

Document processing: ingest + parse + chunk, with guaranteed invariants.

- Zero-knowledge of embeddings or search (never imports embeddy).
- Phase 1 (M1): models, paragraph chunker, ValidatedChunker invariants.
- Extras: `docling`, `tokenizers`.
